<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="per.czt.mapper.UserMapper">
	<!--  <resultMap type="User" id="userMap">
		<id column="user_id" property="user_id"></id>
		<result column="user_account" property="user_account" />
		<result column="user_password" property="user_password" />
		<result column="user_nickname" property="user_nickname" />
		<result column="user_truename" property="user_truename" />
		<result column="user_sex" property="user_sex" />
		<result column="user_email" property="user_email" />
		<result column="user_phoneNum" property="user_phoneNum" />
		<result column="user_birthday" property="user_birthday" />
		<result column="user_photo" property="user_photo" />
		<association property="profession" select="per.czt.mapper.ProfessionMapper.findProfessionById" column="profession_id"  javaType="Profession"/>
		
		<association property="town" javaType="Town">
			<id column="town_id" property="town_id" />
			<result column="town_name" property="town_name" />
			<association property="city" javaType="City">
				<id column="city_id" property="city_id" />
				<result column="city_name" property="city_name" />
				<association property="country" javaType="Country">
					<id column="country_id" property="country_id" />
					<result column="country_name" property="country_name" />
				</association>
			</association>
		</association>
	</resultMap>
	<select id="searchAllUsers" resultMap="userMap">
		select * from user
	</select>-->
	<!-- <resultMap type="User" id="userMap"> <id column="id" property="id"></id> 
		<result column="name" property="name"></result> <result column="age" property="age"></result> 
		</resultMap> <insert id="insertUser" parameterType="User"> insert into user(name,age) 
		values(#{name},#{age}); </insert> <select id="searchAllUser" resultType="User"> 
		select * from user </select> <select id="searchAllUser2" resultMap="userMap"> 
		select * from user </select> <select id="updateUser" parameterType="User" 
		> update user set name=#{name},age=#{age} where id=#{id} </select> <select 
		id="deleteUserById"> delete from user where id=#{value} </select> -->


</mapper>